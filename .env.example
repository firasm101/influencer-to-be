# =============================================================================
# InfluencerToBe - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit the .env file — it is gitignored.
# =============================================================================

# ---- Database (PostgreSQL) --------------------------------------------------
# Local:   postgresql://user:password@localhost:5432/influencer_to_be
# Railway: Provided automatically in Railway dashboard → Variables
DATABASE_URL="postgresql://user:password@localhost:5432/influencer_to_be"

# ---- NextAuth.js ------------------------------------------------------------
# Generate a secret:  openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-random-secret-here"
# Local dev URL (Railway sets this automatically in production)
NEXTAUTH_URL="http://localhost:3000"

# ---- Google OAuth -----------------------------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ---- Anthropic (Claude AI) -------------------------------------------------
# Get your API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY="your-anthropic-api-key"

# ---- RapidAPI (Social Media Data) ------------------------------------------
# Sign up at: https://rapidapi.com/
# Subscribe to the social media APIs used by this project
RAPIDAPI_KEY="your-rapidapi-key"
